name: Build and Deploy

on:
  schedule:
    - cron: '0 */3 * * *'
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: rojadirectaenvivo-biz/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: rojadirectaenvivo-biz

      - name: Build
        run: npm run build
        working-directory: rojadirectaenvivo-biz
        env:
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}

      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy dist/ --project-name=rojadirectaenvivo-biz
        working-directory: rojadirectaenvivo-biz
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}

      - name: Ping search engines
        run: bash scripts/ping-engines.sh
        working-directory: rojadirectaenvivo-biz

      - name: Submit IndexNow
        run: node scripts/indexnow-submit.js
        working-directory: rojadirectaenvivo-biz
        env:
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}
